events {}

http {
    server {
        listen 80;

        # Django
        location /api/chat/ {
            proxy_pass http://django:8000/api/chat/;
        }

        # .NET
        location /api/products {
            proxy_pass http://dotnet:5000/api/products;
        }
    }
}